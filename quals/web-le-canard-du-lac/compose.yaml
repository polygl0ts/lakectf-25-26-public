services:
  web:
    build: 
      context: ./app
      args:
        - flag
    ports:
      - 8085:80
    healthcheck:
      test: ["CMD", "curl", "-f", "http://127.0.0.1:80/"]
      timeout: 1s
      retries: 5
      interval: 10s
