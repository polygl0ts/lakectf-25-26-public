This mod loads the hidden level in game config into the game

Build instructions:
1. modify the UnityManagedPath in the .csproj file
2. run `dotnet build -c release`
3. get `SolveMod.dll` from `bin/Release/netstandard2.1/SolveMod.dll`