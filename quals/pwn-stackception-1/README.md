## IMPORTANT

This challenge consists of three parts:

* **stackception-1:** Manage to send the `win` opcode, get flag
* **stackception-2:** Leak and overflow stack
* **stackception-3:** Leak and overflow but with safestack (likely pushed to finals!)

The `pwn-stackception-1` directory contains everything for all challenges.
`pwn-stackception-2` only exists for the `challenge.yml`.
